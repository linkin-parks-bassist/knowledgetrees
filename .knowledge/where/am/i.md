---
verified_at: '2026-09-12T14:24:07+10:00'
verified_by: codex /root
scope: knowledgetrees repository
source: user request in the active Codex session; filesystem inspection
verification: Confirmed the repository layout, visible example corpus, knowledge-first installer, hard-linked compatibility bootstrap design, verifier, local Git state, and public unpushed remote.
review_when: Recheck when repository purpose, layout, or publication state changes.
---

This is the project knowledge root for the `knowledgetrees` public repository being
assembled in this working directory.

The repository will explain knowledge trees, include the installed compatibility
skill, and provide a visible `example/` tree containing knowledge-tree material plus
adapted planning, specification, update, and clarification guidance. The `install`
script installs that knowledge first and creates skill-shaped hard links only for
current harness bootstrap compatibility. The current
publication boundary is explicit: prepare and commit locally, create the public
GitHub repository for its intended owner, then stop for the repository owner's review before
the first push.

Use `what/is/the/spec.md` for the acceptance contract, `what/is/the/plan.md` for the
approved workflow, `what/is/the/state.md` for verified present state, and
`what/is/next.md` for the next action. Use `example/where/am/i.md` to enter the
public example corpus.
