---
status: green
revised_at: "2026-09-20T10:55:17+10:00"
---

The operational root is `.knowledge/`; `example/` is the public distributable specimen with an empty orientation. `tools/kt` is installed byte-for-byte from this repository. It supports body-only add/rewrite, flat lifecycle metadata, optional expiry and `verifiable`, access-controlled lookup, and proof checks. The installer preserves personal orientation and access policy, renders public guidance into the global root, and hardlinks compatibility skills.

Startup hooks supply the complete `kt boot` output in Codex, OpenCode, and Copilot CLI. The installer no longer creates a home `AGENTS.md`; it removes only its legacy marked block and deletes the file if otherwise empty. The home file was block-only and is gone. The repository's duplicate `AGENTS.md` is removed; its guidance is owned by the canonical global procedure and this project's orientation and maintenance leaves. The installed Copilot handler and hook configuration now include `sessionStart`. Handler protocol tests and a direct installed-handler invocation produced the complete boot output through its final proof summary. Live Copilot model reception was not observed. Two pre-existing user edits to the OpenCode hook and its test remain untouched.

Current guidance treats known stale or contradictory active knowledge as an immediate repair obligation. A zero-exit `kt prove` run means only that selected leaves had no brown-level failure detected at that time; yellow may remain and green can include proof-free leaves. Coverage and predicate quality need agent review even with `verifiable: true`. The README, public example, local project leaves, and installed global guidance have been reconciled. Installer and hook integration tests pass. Local, example, and global proof sweeps report zero yellow and brown; installed instruction sync reports zero drift; Git whitespace checks pass. These checks do not certify semantic truth in unrelated trees.

A deferred event-triggered expiry idea is recorded at `what/is/the/proposed/event/triggered/expiry/feature.md`; no hook or CLI behavior was changed for it.

The 2026-09-20 flat-metadata migration audited 11 then-existing active, non-private roots and changed 179 leaves while preserving answer bodies and inodes. Its backup and post-change manifest remains at `/home/david/.local/share/knowledgetrees/metadata-backups/2026-09-20-hard-break/manifest.json`. Archived snapshots and force-private roots were outside that audit; one registered agent-ecosystem root was absent. These are historical audit bounds, not a claim that all present prose is checked.
