---
status: green
revised_at: "2026-09-22T09:40:21+10:00"
---

The operational root is `.knowledge/`; `example/` is the public distributable specimen with an empty orientation. `tools/kt` is installed byte-for-byte from this repository. It supports body-only add/rewrite, flat lifecycle metadata, optional expiry and `verifiable`, access-controlled lookup, and proof checks. The installer preserves personal orientation and access policy, renders public guidance into the global root, and hardlinks compatibility skills into the shared `~/.agents/skills/` catalog. Codex-local knowledge-tree skill copies and exact explicit registrations are retired so Codex discovers each skill once; unrelated skills and configuration are preserved.

Startup hooks supply `kt info` context in Claude Code, Codex, OpenCode, and Copilot CLI. As of 2026-09-21 the installer deliberately wires only startup events: Claude Code and Codex `SessionStart`, Copilot `sessionStart`, and OpenCode system-context initialization. Reinstallation removes this project's formerly managed prompt, post-tool, failure, stop, and idle integrations while preserving unrelated hooks. The shared handler retains dormant compatibility code, but installed definitions do not invoke it; there are no managed failure heuristics, call counters, capture-review continuations, task-end blocks, or extra model turns. Focused installer and OpenCode adapter tests cover both retirement and startup retention. Claude Code still receives an instruction to call `kt_info` (or run `kt info` without tools) when output exceeds its 9,500-byte hook limit.

`kt info` no longer requires a local tree: with no `./.knowledge`, or one lacking `where/am/i.md`, it prints a one-line note, the global orientation, and proves the global (or partial local) root; it still fails when the global procedure is unreachable, access needs approval, or proofs are brown. Tests cover both fallbacks.

A second full audit after the access work cross-checked every document against code-derived lists (16 tools, four prompts, all CLI subcommands), a fresh-home install (configs parse, all four harnesses registered, hardlinks share an inode, installed server lists 16 tools and answers a lookup, hook handler startup/failure/stop behave), and every installer mode on the real home. It fixed the README prompt count, the installer's outdated MCP wording, the `kt register` gap in the usage leaf, and a private project name and a home path in this leaf's earlier text; earlier pushed history still contains them. A 2026-09-20 documentation audit reconciled the README, public example, and local leaves with four-harness hooks, the MCP tools, the global-root fallback, and the Claude Code context cap (skill directories, installer guarantees, test commands, harness lists, and failure-detection claims had been stale); the untracked instruction-sync check reports zero drift.

The MCP server `tools/kt-mcp` (20 tools: `kt_info`, `kt_lookup`, `kt_find`, `kt_grep`, `kt_read`, `kt_rewrite`, `kt_edit`, `kt_undo`, `kt_add`, `kt_rm`, `kt_mv`, `kt_init`, `kt_renew`, `kt_dict`, `kt_roots`, `kt_prove`, `kt_status`, `kt_access_status`, `kt_access_request`, `kt_access_revoke`; with annotations, four prompts, whole-answer rewrite, atomic multi-edit, and access approval by MCP elicitation; combine and policy commands remain deliberately unexposed) is installed and registered for Claude Code, Codex, OpenCode, and Copilot CLI; its owner is `example:how/to/expose/structured/knowledge-tree/edits/across/local/agent/harnesses.md`, tracked in the repository and rendered into the global root by the installer. A live Claude Code session read, edited, and had a stale revision rejected through the original three tools; a later live Claude Code session also ran `kt_lookup`, `kt_find` (a miss returned as data, not an error), and a `kt_add` dry run; `kt_dict`, `kt_roots`, `kt_prove`, and non-dry-run `kt_add` are covered by tests only, and the owner reported the tools working in live Codex and OpenCode sessions (2026-09-20, not independently observed), and Copilot CLI is configured but not observed live. A later root-selection probe found that a user-scoped server started from home could resolve `local:` against home despite `CLAUDE_PROJECT_DIR` naming the project. The server now selects `KT_MCP_PROJECT_DIR` or `CLAUDE_PROJECT_DIR` before handling calls; the simulated installed-server probe resolved a leaf from a private project tree when launched from the home directory. Other harnesses' local-root behavior remains to be qualified. `kt grep` (literal or regex, access-filtered) and `kt status` (non-green leaves with reasons, read-only) are new CLI commands, and `kt access` is split into `resolve_access_root` and `apply_access_decision`. Elicitation, undo, atomic multi-edit, and the other new tools are covered by protocol tests with a fake client only; the approval prompt was observed live in interactive Claude Code on 2026-09-20 (owner-observed): a request on a throwaway root prompted the owner, the accept persisted a project grant, and `kt_access_revoke` narrowed it again with no second prompt; A Codex request on 2026-09-21 returned `action=decline` without displaying any prompt to the owner. The server now reports the client action without falsely claiming a user refusal, provides the terminal fallback, and permits another request; the reason Codex did not display elicitation remains unverified. OpenCode and Copilot CLI have not been observed. A 2026-09-20 probe found that `kt` never cleaned up its access registry: deleted trees left grants behind and a different tree recreated at the same path silently inherited the old approval. `kt` now prunes stale project grants, everywhere-allowed registrations, and auto-named ask registrations on each run (the first run against the owner's real registry removed 21 dead `root-<hash>` ask entries left by earlier test runs; a backup was saved first), `kt grants` explains each root's access, and `kt access ROOT revoke` and `kt_access_revoke` give access back; the corrupt-config error message still does not name the file. The installer no longer creates a home `AGENTS.md`; it removes only its legacy marked block and deletes the file if otherwise empty. The home file was block-only and is gone. The repository's duplicate `AGENTS.md` is removed; its guidance is owned by the canonical global procedure and this project's orientation and maintenance leaves. The installed Copilot handler and hook configuration now include `sessionStart`. Handler protocol tests and a direct installed-handler invocation produced the complete info output through its final proof summary. Live Copilot model reception was not observed. The two previously uncommitted user edits to the OpenCode hook and its test (removal of the piped `kt info`/`kt init` guard) are committed separately as 503b6cc.

Agent tools are lean and the guidance is MCP-first. `kt init` now registers the new tree under `ask` while leaving cross-project access ungranted. A failed MCP elicitation can be followed by an exact project-scoped CLI grant when the user explicitly authorizes that root and scope in conversation; a real project grant was checked without enabling the global permissions bypass. Whole-leaf `kt_read` and exact lookup hits return the complete answer plus a final SHA-256 `Revision:` line, with no front matter, timestamps, paging, truncation, or partial-read operation; ranked excerpts are selectors and provide no hash. `kt_rewrite` is the standard editing tool and requires that explicit read hash, with stale values rejected before the CLI's locked rewrite. `kt_edit` is reserved for economy on a tiny surgical exact-match change based on the server's remembered whole answer, and `kt_renew` likewise requires a remembered whole read. Lifecycle: status moves down by itself and up only by hand. `kt prove` never raises a status (the exception is a `verifiable: true` leaf whose proofs all pass); `kt add` starts a leaf green (with `checked_at` when `--expires-every` is given), a rewrite keeps status and `checked_at`, `kt combine` takes the worst source status, and `kt renew ADDRESS HASH` (tool `kt_renew`) records `checked_at`, clears any brown, and re-runs that leaf's own proofs. The `leaf_health` refactor also fixed a latent `kt status` crash on leaves with malformed metadata (it joined `(line, message)` tuples as strings); malformed reasons read `line N: message`. The core procedure, kt reference, MCP, metadata, maintenance, add, rewrite, proof, traversal, access, hook, and ingestion example leaves, the README, and the hook reminders name the tools first and the shell as fallback. Covered by `tests/test-mcp.py` and `tests/test-metadata.py`, including hash display, rewrite dry-run, malformed and stale hash rejection, and the absence of partial-read parameters; every test suite passes. Installed on 2026-09-21 (the sync script refreshed the core leaves with a backup under `~/.local/state/knowledgetrees/instruction-backups/`, then `./install --force` deployed the tools, hooks, remaining leaves, and skills); harnesses must restart to load it and none of it has been observed in a live harness session. The startup output is about 11 KB, above Claude Code's 9,500-byte hook cap, so Claude Code receives the call-`kt_info` instruction rather than the full text.

Current guidance treats known stale or contradictory active knowledge as an immediate repair obligation. A zero-exit `kt prove` run means only that selected leaves had no brown-level failure detected at that time; yellow may remain and green can include proof-free leaves. Its default output is two dynamically aligned text lines: `Leaves:` with total, green, yellow, and brown counts; `Proofs:` with total, valid, and failed counts followed by `SUCCESS` or `FAIL`. Corresponding counts align vertically, and the result starts under the brown count. Coverage and predicate quality need agent review even with `verifiable: true`. The README, public example, local project leaves, and installed global guidance describe this format. All Python and OpenCode integration tests pass. The source and installed CLI and MCP server match byte for byte; local, example, and installed global proof sweeps report zero yellow and brown. Installed instruction sync reports zero drift, and Git whitespace checks pass. These checks do not certify semantic truth in unrelated trees.

A deferred event-triggered expiry idea is recorded at `what/is/the/proposed/event/triggered/expiry/feature.md`; no hook or CLI behavior was changed for it.

The 2026-09-20 flat-metadata migration audited 11 then-existing active, non-private roots and changed 179 leaves while preserving answer bodies and inodes. Its backup and post-change manifest remains at `~/.local/share/knowledgetrees/metadata-backups/2026-09-20-hard-break/manifest.json`. Archived snapshots and force-private roots were outside that audit; one registered agent-ecosystem root was absent. These are historical audit bounds, not a claim that all present prose is checked.
