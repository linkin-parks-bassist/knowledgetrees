---
status: green
revised_at: "2026-09-21T09:03:53+10:00"
---

Restart the harnesses to load the lean output, `kt_renew`, read-before-edit, and the MCP-first guidance (installed 2026-09-21), then observe in a live session that a yellow leaf shows its notice, `kt_renew` greens it, and agents reach for the tools rather than the shell. Then trigger `kt_access_request` and `kt_access_revoke` (including scope all, which asks for confirmation) in interactive Codex and OpenCode sessions and record whether each client shows a prompt and the change persists (Claude Code is verified); then observe the newer MCP tools through a live Copilot CLI session (the original tools are owner-reported working in Codex and OpenCode), including correct project-root resolution (the MCP server is installed and registered for all four harnesses; only Claude Code has been exercised by the agent), observe a live failure reminder in an interactive Claude Code session, and decide whether to slim `kt info` output (procedure about 6 KB, orientation about 2 KB, dictionary about 2 KB, growing with the tree) so Claude Code can receive it in full instead of through the run-it-directly instruction; other harnesses' hook-context limits are unverified. The event-triggered expiry idea remains deferred as requested. Repair any newly discovered stale active leaf before reliance.
