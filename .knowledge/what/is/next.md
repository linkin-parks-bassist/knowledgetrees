---
status: green
revised_at: "2026-09-22T09:40:20+10:00"
---

Restart Codex so it rebuilds its skill catalog, then confirm each knowledge-tree skill appears once from `~/.agents/skills/`. The installed `kt prove` already shows the aligned `Leaves:` and `Proofs:` summary with `SUCCESS` or `FAIL`. In fresh live harness sessions, verify startup-only hook delivery and exercise whole reads with Revision hashes, `kt_rewrite` success and stale-hash rejection, yellow-leaf renewal, and the newer MCP tools across Codex, OpenCode, and Copilot CLI. Investigate why Codex returned elicitation decline without rendering a prompt; when the user explicitly authorizes the exact root and project scope, an agent can confirm the matching `kt access` fallback interactively; otherwise the user can run it. Trigger `kt_access_request` and `kt_access_revoke` interactively in OpenCode and Copilot CLI where still unobserved. Decide whether to slim `kt info` so Claude Code can receive it in full instead of the run-it-directly instruction. The event-triggered expiry idea remains deferred. Repair any newly discovered stale active leaf before reliance.
