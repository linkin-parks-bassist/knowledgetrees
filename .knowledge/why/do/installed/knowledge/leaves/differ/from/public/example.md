---
status: green
revised_at: "2026-09-18T12:22:12+10:00"
---

The installed global knowledge tree retains original personal formulations from
before the public repository was created. The repository example contains sanitized
public adaptations committed mainly on 2026-09-12. A full installer therefore sees
matching semantic owners with different bytes; this is mostly historical divergence,
not evidence that recent agents edited every leaf.

In the 2026-09-15 audit, 41 example-owned installation targets conflicted. Nineteen
had identical bodies and differed only in metadata or proof timestamps. After proof
timestamp normalization, 21 had different bodies. Their installed provenance names
original personal explanations and local observations, while their example
provenance names sanitized public adaptations. The installed tree has no Git history
that can rule out later individual edits, but the provenance and example commit
history show that the main fork predates the current work.

Do not force-install the entire example merely to deploy CLI or hook changes: that
would replace these original global formulations with public adaptations. Use
`./install --hooks-only --force` for infrastructure, the curated manual refresh for
core instructions, and reconcile other semantic owners deliberately. The manual
refresh must render public example scope as `personal global`, matching the installer;
raw copying creates false scope conflicts. Project orientation and spine leaves are
excluded from example installation, and unrelated global paths are not overwritten.


On the owner's 2026-09-15 instruction, the full forced installer replaced all 51
installer-managed reusable leaves with current rendered example guidance. The
historical divergence is therefore resolved. The manual refresh now renders scope
like the installer and ignores successful proof timestamp refreshes during drift
checks. Future conflicts should represent actual content/metadata drift or unmanaged
installation state rather than this historical fork.
