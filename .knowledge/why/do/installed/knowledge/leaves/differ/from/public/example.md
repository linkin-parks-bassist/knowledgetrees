---
status: green
revised_at: "2026-09-24T10:06:25+10:00"
---

Installer-managed global leaves are rendered copies of public `example/` guidance, with the public scope adapted to personal global scope. Proof markers are exact, timeless answer content and do not differ after checks. The installed root also contains personal leaves and orientation outside the example installation set. A managed answer that differs substantively from current source needs reconciliation; do not assume historical divergence explains it.

Check curated instruction drift with `python3 sync-kt-instructions.py --check`, compare the full source and installed owner, then refresh the intended scope with the sync script or installer. Preserve personal and project-specific knowledge. The earlier pre-publication fork of managed leaves was resolved by a full forced installation; it is historical evidence, not the current default state. Source evidence: `install` renders example leaves, and `sync-kt-instructions.py` defines the curated comparison and refresh.
